-- searching all records from a table, sorting the result alphabetically
SELECT * FROM tableName
ORDER BY colName;

-- searching all records from a table, sorting the result reversed alphabetically
SELECT * FROM tableName
ORDER BY colName DESC;

-- searching all records from a table, sorting the result alphabetically
-- by two or more columns
SELECT * FROM tableName
ORDER BY colName1, colName2;
